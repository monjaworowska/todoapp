const initialState = {
  todos: [
    { id: 0, task: "Taks 1", done: false },
    { id: 1, task: "Task 2", done: false },
    { id: 2, task: "Task 3", done: false },
  ],
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
